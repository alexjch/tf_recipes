#cloud-config
users:
  - default
  - name: ${username}
    gecos: Gravitron User
    shell: /bin/bash
    groups: [wheel]
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: true
    ssh_authorized_keys:
      - ${ssh_pub_key}

ssh_pwauth: false
disable_root: true
